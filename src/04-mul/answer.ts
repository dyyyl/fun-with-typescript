// Write a function `mul` that multiplies two numbers
/**
 * Binary function that returns the product of two numbers
 * @param multiplicand
 * @param multiplier
 */
export const mul: Binary = (multiplicand, multiplier) => multiplicand * multiplier;
